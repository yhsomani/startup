/**
 * Placeholder Companies Page Component
 */

import React from 'react';
import { Box, Typography, Card, CardContent, CircularProgress } from '@mui/material';

const Companies = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Companies
      </Typography>
      <Card>
        <CardContent>
          <Typography>
            Companies page - Coming soon with company listings, search, and filtering capabilities
          </Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default Companies;