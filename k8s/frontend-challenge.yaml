apiVersion: v1
kind: Service
metadata:
  name: frontend-challenge-staging
  namespace: talentsphere-staging
  labels:
    app: frontend-challenge
    tier: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: frontend-challenge
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-challenge-staging
  namespace: talentsphere-staging
  labels:
    app: frontend-challenge
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-challenge
  template:
    metadata:
      labels:
        app: frontend-challenge
        tier: frontend
    spec:
      containers:
      - name: frontend-challenge
        image: talentsphere/frontend-challenge:staging
        ports:
        - containerPort: 80
        env:
        - name: NODE_ENV
          value: "staging"
        - name: VITE_API_BASE_URL
          value: "http://api-gateway:8000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-challenge-production
  namespace: talentsphere-production
  labels:
    app: frontend-challenge
    tier: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: frontend-challenge
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-challenge-production
  namespace: talentsphere-production
  labels:
    app: frontend-challenge
    tier: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend-challenge
  template:
    metadata:
      labels:
        app: frontend-challenge
        tier: frontend
    spec:
      containers:
      - name: frontend-challenge
        image: talentsphere/frontend-challenge:latest
        ports:
        - containerPort: 80
        env:
        - name: NODE_ENV
          value: "production"
        - name: VITE_API_BASE_URL
          value: "https://talentsphere.com"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5