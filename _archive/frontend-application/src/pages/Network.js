/**
 * Placeholder Network Page Component
 */

import React from 'react';
import { Box, Typography, Card, CardContent } from '@mui/material';

const Network = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Professional Network
      </Typography>
      <Card>
        <CardContent>
          <Typography>
            Network page - Coming soon with connections, messaging, and professional networking features
          </Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default Network;