{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://talentsphere.com/schemas/events/LessonCompleted.json",
  "title": "LessonCompleted",
  "description": "Event fired when a user completes a lesson",
  "type": "object",
  "required": ["messageId", "timestamp", "data"],
  "properties": {
    "messageId": {
      "type": "string",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "data": {
      "type": "object",
      "required": ["userId", "lessonId", "courseId"],
      "properties": {
        "userId": {
          "type": "string",
          "format": "uuid"
        },
        "lessonId": {
          "type": "string",
          "format": "uuid"
        },
        "courseId": {
          "type": "string",
          "format": "uuid"
        },
        "pointsAwarded": {
          "type": "integer",
          "default": 25
        }
      }
    }
  }
}
